namespace SQLEngine.Tests;

public partial class Test_Query_Builder_Sql_Server
{
    //TODO Below queries needs to be thinking of how to write the expression

    /* 
    
    Sql:
    select * from Users where LEN(Name)=5; --custom function call on search
    
    C#:
    q
    .Select
    .From("Users")
    .Where(???)


    */
}